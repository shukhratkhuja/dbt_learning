[project]
name = "dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "s.fayzullaev", email = "shukhrat.fayzulla@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dbt-core>=1.12.3",
    "dbt-databricks>=1.12.5",
    "dbt-postgres>=1.11.0",
]

[project.scripts]
dbt = "dbt:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
